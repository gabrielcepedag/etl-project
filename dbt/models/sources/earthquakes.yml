sources:
  - name: earthquakes
    database: RAW
    schema: public

    tables:
      - name: raw_earthquakes
        columns:
          - name: id
            description: "Unique earthquake identifier"
            tests:
              - not_null
              - unique
